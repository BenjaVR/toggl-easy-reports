import { createStyles } from "../../utilities/styles";

export const styles = createStyles({
    loadingSpinner: {
        position: "absolute",
        left: "50%",
        transform: "translateX(-50%)",
        bottom: "10px",
    },
});
