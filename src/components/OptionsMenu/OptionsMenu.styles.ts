import { createStyles } from "../../utilities/styles";

export const styles = createStyles({
    icon: {
        color: "white",
        fontSize: 24,
    },
    menu: {
        padding: 12,
    },
    dropdownOverlay: {
        width: 485,
        maxWidth: "100%",
    },
});
